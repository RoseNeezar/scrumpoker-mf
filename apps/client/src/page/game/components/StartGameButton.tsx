import React from "react";

type Props = {};

const StartGameButton = (props: Props) => {
  return (
    <button className="btn-primary btn m-auto my-10 w-fit">New round</button>
  );
};

export default StartGameButton;
